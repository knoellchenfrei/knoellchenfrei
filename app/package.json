{
  "name": "parkingzone",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "artifact": "pnpm --filter @parkingzone/web build:artifact && DIST_DIR=../../apps/web/dist-artifact pnpm --filter @parkingzone/ingest build-artifact",
    "typecheck": "pnpm -r typecheck",
    "test:coverage": "pnpm -r test:coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
