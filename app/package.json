{
  "name": "knoellchenfrei",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "test": "pnpm -r test",
    "artifact": "pnpm --filter @knoellchenfrei/web build:artifact && DIST_DIR=../../apps/web/dist-artifact pnpm --filter @knoellchenfrei/ingest build-artifact",
    "typecheck": "pnpm -r typecheck",
    "test:coverage": "pnpm -r test:coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^7.0.2",
    "vitest": "^3.2.4"
  }
}
